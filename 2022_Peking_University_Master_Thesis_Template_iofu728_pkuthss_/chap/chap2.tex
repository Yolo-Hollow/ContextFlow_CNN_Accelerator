\chapter{背景与相关工作}
\label{chap:related}

本章介绍本文设计所依赖的基础概念，包括卷积计算模型、INT8 量化、systolic array 数据流、YOLOv3-tiny 单尺度网络结构、MPSoC 平台特征以及 FPGA CNN 加速器相关研究。与传统背景介绍不同，本章重点服务于后文的数据流与调度优化：真实网络的性能瓶颈来自层形状、K pass 数量、输出通道分块、空间 tile、部分和反馈和 PS/PL 数据搬运的共同作用。

\section{卷积计算与分块表示}

给定输入特征图 $I \in \mathbb{R}^{H \times W \times C_{in}}$、权重 $W \in \mathbb{R}^{K_h \times K_w \times C_{in} \times C_{out}}$ 和偏置 $B$，标准二维卷积可以表示为
\begin{equation}
O[y,x,c_o] = B[c_o] + \sum_{c_i=0}^{C_{in}-1} \sum_{k_y=0}^{K_h-1} \sum_{k_x=0}^{K_w-1}
I[yS+k_y-P, xS+k_x-P, c_i] \cdot W[k_y,k_x,c_i,c_o],
\label{eq:conv2d}
\end{equation}
其中 $S$ 为 stride，$P$ 为 padding。将卷积窗口和输入通道展开后，可得到 GEMM 形式：
\begin{equation}
O[p,c_o] = B[c_o] + \sum_{k=0}^{K_{total}-1} X[p,k] \cdot W[k,c_o],
\quad K_{total}=C_{in}K_hK_w .
\label{eq:gemm}
\end{equation}

本文硬件按照三个维度组织计算：$K$ 维分块、输出通道分块和空间分块。若 systolic array 每次处理 $K_{tile}$ 个展开通道，则一个卷积层需要
\begin{equation}
N_{kpass}=\left\lceil \frac{K_{total}}{K_{tile}} \right\rceil
\end{equation}
轮 K pass。若每个输出通道块包含 $Cout_{tile}$ 个通道，则输出通道块数为
\begin{equation}
N_{cout}=\left\lceil \frac{C_{out}}{Cout_{tile}} \right\rceil .
\end{equation}
真实层的调度规模由 $N_{tile}\times N_{cout}\times N_{kpass}$ 决定。后端层如 $13\times13\times512 \rightarrow 13\times13\times1024$ 虽然空间尺寸较小，但 $K_{total}$ 和 $N_{cout}$ 很大，因此容易暴露 pass 边界和 partial-PSUM 反馈瓶颈。

\section{INT8 量化与后处理}

边缘 CNN 推理通常采用 INT8 量化降低存储带宽和乘加资源消耗。浮点值 $r$ 与量化整数 $q$ 之间可表示为~\cite{jacob2018quantization}
\begin{equation}
q=\mathrm{clamp}\left(\mathrm{round}\left(\frac{r}{S}\right)+Z,q_{min},q_{max}\right),
\label{eq:quant}
\end{equation}
其中 $S$ 为 scale，$Z$ 为 zero point。卷积内部使用 INT8 输入和权重，乘加结果累加到 INT32 partial sum。输出时通过逐通道 multiplier、shift 和 output zero point 完成重量化：
\begin{equation}
q_{out}=\mathrm{clamp}\left(\left((psum \cdot m) \gg s\right)+Z_{out},0,255\right).
\label{eq:requant}
\end{equation}

本文加速器将重量化、activation LUT 和可选 maxpool 集成在 PL 端，从而减少每层输出回到软件后再处理的开销。A53 软件只负责写入每个输出通道 lane 的量化参数和 activation LUT 字节。

\section{Systolic Array 与数据流}

Systolic array 由规则排列的处理单元（Processing Element, PE）组成，数据在相邻 PE 之间按节拍传播，适合矩阵乘和卷积等规则计算~\cite{kung1982systolic}。不同驻留策略会形成不同数据流：weight-stationary 将权重保持在 PE 内部，input-stationary 保持输入局部性，output-stationary 保持 partial sum，row-stationary 则试图综合利用多类数据复用~\cite{sze2017efficient,chen2016eyeriss}。

本文采用 weight-stationary 风格。每个 K pass 开始前，软件或 batch stream 将当前 $K_{tile}\times Cout_{tile}$ 权重块送入片上权重存储；随后 IFM/window 向量流入阵列，PE 执行 INT8 乘法并累加到 INT32 partial sum。该选择的优点是权重加载和阵列计算语义清晰，便于在 RTL 中实现稳定的 AXI-Stream 边界；代价是多 K pass 层需要显式 partial-PSUM 反馈路径。

\section{YOLOv3-tiny 单尺度网络}

本文面向的模型是 YOLOv3-tiny 的单尺度链路。输入为 $416\times416\times3$ 量化 RGB 图像，硬件链式执行 Conv0 到 Conv9，最终输出 $13\times13\times24$ 检测头。表~\ref{tab:yolo_layers} 给出本文 runtime 使用的层级形状和调度规模。

图~\ref{fig:yolov3_tiny_single_scale} 展示 YOLOv3-tiny 的单尺度检测链路。本文实现和验证的 Conv0--Conv9 对应该链路中从输入到 $13\times13$ 检测头的顺序卷积路径，保留 3x3 卷积、1x1 检测头、池化和逐层后处理等结构特征，作为 LASA 层自适应机制与后续数据流优化的实验对象。

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{\detokenize{img/picture/单尺度yolov3_tiny.png}}
\caption{YOLOv3-tiny 单尺度检测链路}
\label{fig:yolov3_tiny_single_scale}
\end{figure}

\begin{table}[htb]
\centering
\caption{Conv0--Conv9 单尺度链路层参数}
\label{tab:yolo_layers}
\begin{tabular}{lrrrrrr}
\toprule
层名 & IFM & $C_{in}$ & $C_{out}$ & Kernel & K passes & Cout blocks \\
\midrule
conv0\_pool & $416\times416$ & 3 & 16 & 3x3 & 2 & 1 \\
conv1\_pool & $208\times208$ & 16 & 32 & 3x3 & 8 & 2 \\
conv2\_pool & $104\times104$ & 32 & 64 & 3x3 & 16 & 4 \\
conv3\_pool & $52\times52$ & 64 & 128 & 3x3 & 32 & 8 \\
conv4\_pool & $26\times26$ & 128 & 256 & 3x3 & 64 & 16 \\
conv5 & $13\times13$ & 256 & 512 & 3x3 & 128 & 32 \\
conv6 & $13\times13$ & 512 & 1024 & 3x3 & 256 & 64 \\
conv7 & $13\times13$ & 1024 & 256 & 1x1 & 57 & 16 \\
conv8 & $13\times13$ & 256 & 512 & 3x3 & 128 & 32 \\
conv9 & $13\times13$ & 512 & 24 & 1x1 & 29 & 2 \\
\bottomrule
\end{tabular}
\end{table}

从表中可以看到，后端 13x13 层虽然空间尺寸固定，但 K passes 和 Cout blocks 显著增加，是本文优化 partial-PSUM、raw-HWC replay 和 pass-level prefetch 的主要目标。

\section{MPSoC 平台与 AXI 接口}

Kria KV260 使用 Xilinx Zynq UltraScale+ MPSoC，包含 ARM Cortex-A53 PS 和 XCK26 PL 逻辑资源。PS/PL 之间通过 AXI4-Lite、AXI4-Stream 和 AXI4-MM 互连进行控制和数据搬运~\cite{arm2021axi,xilinx2022zynq}。本文采用 AXI-Lite 作为寄存器配置接口，并使用四路 AXI DMA：bias、weight、IFM 三路从 DDR 到 PL，OFM 一路从 PL 到 DDR。

AXI-Stream 的 TVALID/TREADY 握手机制有利于处理 PL 内部背压，但也要求 RTL 对 TLAST、TKEEP、packet count 和 DMA length 做严格约束。本文的 batch stream、raw-HWC cache 和 OFM debug stream 都建立在该边界之上。

\section{相关工作比较}

现有 FPGA CNN 加速器工作在自动化映射、阵列生成、模型压缩和单层吞吐上取得了大量成果~\cite{qiu2016going,guo2019angeleye,wei2017s2a,ma2018rtlcompiler,ding2022tinyyolo}。与这些工作相比，本文的差异主要在三个方面。

首先，本文以完整 Conv0--Conv9 链式推理作为验证对象，而不仅是单层或合成测试。每层输出都需要作为下一层输入，golden 数据也必须遵循同一条 RTL semantic 链路。其次，本文关注真实 MPSoC 部署中的数据搬运问题，包含 A53 runtime、AXI DMA、DDR 图像包、OFM packet 重排和 YOLO decode。最后，本文通过性能计数器对瓶颈迁移进行解释，强调优化为什么有效以及下一步瓶颈在哪里，而不是只报告单个最终延迟。

因此，本文的贡献更接近系统型和数据流优化型研究：在资源受限的可编程平台上，将真实 CNN 链路、RTL 数据通路、软件调度和板级性能诊断统一起来。
